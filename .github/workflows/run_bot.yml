name: Hourly Notice Checker

on:
  schedule:
    - cron: '0 * * * *' # প্রতি ঘণ্টায় রান হবে
  workflow_dispatch:

jobs:
  run-bot:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout Code
        uses: actions/checkout@v5 # 👈 v4 এর জায়গায় v5 দিন

      - name: Set up Python
        uses: actions/setup-python@v6 # 👈 v5 এর জায়গায় v6 দিন
        with:
          python-version: '3.10'

      - name: Install Dependencies
        run: |
          python -m pip install --upgrade pip
          pip install requests beautifulsoup4 urllib3

      - name: Run Script
        env:
          TELEGRAM_BOT_TOKEN: ${{ secrets.TELEGRAM_BOT_TOKEN }}
          TELEGRAM_CHAT_ID: ${{ secrets.TELEGRAM_CHAT_ID }}
        run: python xsjobnotice_bot.py

      - name: Commit and Push History
        run: |
          git config --global user.name "github-actions[bot]"
          git config --global user.email "github-actions[bot]@users.noreply.github.com"
          git add downloaded_history.txt
          git commit -m "Update downloaded notices history" || exit 0
          git push
